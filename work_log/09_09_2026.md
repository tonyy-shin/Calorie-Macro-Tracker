# 09/09/2026

## Phase 5 — Logging
- Reviewed and finalized `create_meal_log.sql`: `id`, `user_id BIGINT REFERENCES users(id)`,
  `log_date DATE`, `meal_name`, `source_type`, `recipe_id BIGINT REFERENCES recipes(id)`,
  `portions`, snapshot macro columns (`kcal`, `protein_g`, `carbs_g`, `fat_g` all NOT NULL),
  `logged_at TIMESTAMPTZ DEFAULT now()`
- Reviewed and finalized `create_meal_log_items.sql`: `id`, `meal_log_id BIGINT NOT NULL
  REFERENCES meal_log(id)`, `food_id`, `food_origin`, `grams`
- Held off running both until `recipes` existed, since `meal_log.recipe_id` FKs into it

## Phase 6 — Recipes
- Created `create_recipes.sql`: `id`, `user_id BIGINT NOT NULL REFERENCES users(id)`,
  `name`, `yield_portions INTEGER`, `created_at TIMESTAMPTZ DEFAULT now()`
- Created `create_recipe_items.sql`: `id`, `recipe_id BIGINT NOT NULL REFERENCES recipes(id)`,
  `food_id`, `food_origin`, `grams`

## Execution
- Ran all four tables in dependency order: `recipes` → `recipe_items` → `meal_log` →
  `meal_log_items`
- Verified via `information_schema.columns` — all 27 columns across the four tables match
  the schema exactly (types and nullability confirmed)

## Next
- Recipe per-portion query (join `recipes` → `recipe_items` → `foods`)
- Ad hoc logging path (search → add grams → save into `meal_log` + `meal_log_items`)
- Daily rollup query
