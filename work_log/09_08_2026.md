# 09/08/2026 — Phase 5: Meal Log Table

## Summary
Started Phase 5: created the meal_log table, the header table for
logged meals (recipe-based or ad hoc). meal_log_items still to come.

## Work done
- Wrote create_meal_log.sql in src/sql/gold/
- Caught and fixed several bugs before running:
  - `GENERATED ALWAYS AS IDENTITY` keyword order (tripped up multiple
    times — GENERATED, then ALWAYS, then AS, then IDENTITY)
  - user_id needed to be BIGINT REFERENCES users(id), not TEXT
  - log_date is DATE (the date being logged for), not TIMESTAMPTZ
    DEFAULT now() — that's what logged_at is for
  - recipe_id syntax error (`BIGINT::NULL` isn't valid — nullable FK
    just needs no NOT NULL constraint)
  - portions is DOUBLE PRECISION, not TEXT
  - kcal/protein_g/carbs_g/fat_g need NOT NULL — they're snapshot
    values computed at log time, always present
- Need to create recipe table for recipe(id) to run the code

## Notes
- recipe_id REFERENCES recipes(id) will fail if recipes doesn't exist
  yet. If it errored, the FK constraint needs to be dropped and added 
  later via ALTER TABLE.

## Next
Write create_meal_log_items.sql, then build the ad hoc logging path
(search -> add grams -> save) before the daily rollup query.
