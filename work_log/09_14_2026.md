# 09/14/2026

## Done
- Wrote `search_foods(conn, search_term, user_id)` in `meal_logging.py` —
  parameterized query against the `foods` view, filters on
  `name ILIKE` + `(origin = 'source' OR user_id = %s)`, `LIMIT 20`
- Renamed `logging.py` → `meal_logging.py` (was shadowing Python's
  stdlib `logging` module, which broke `psycopg`'s import)
- Tested `search_foods()` interactively (`python3 -i`) with "chicken" —
  20 rows returned, columns and origin filter correct

## Next
- Write `log_ad_hoc_meal()`: sum macros across selected items, insert
  into `meal_log` (RETURNING id), then `meal_log_items` per item, in
  one transaction
- Re-test `search_foods()` with a real manual-entry row + user_id 3 to
  confirm the `origin = 'manual'` branch
- After ad hoc logging: daily rollup query, then Phase 7 :)
