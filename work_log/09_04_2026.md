# 09/04/2026

## Gold layer: foods_source merge (Phase 2 complete)

- Built `foods_source` as UNION ALL of `usda_food_gold` and `off_product_gold`,
  USDA wins cross-source barcode collisions; OFF rows with
  NULL barcode pass through unmatched.
- Excluded 336,139 OFF rows (7.4%) with NULL `product_name` — unusable without
  a name regardless of macro completeness. Documented as a data quality
  decision, not a silent drop.
- Root caused a slow merge (10+ min, DataFileRead wait): no index existed on
  `usda_food_gold.barcode`, forcing the NOT EXISTS anti join into a per row
  scan instead of a hash join. Added `idx_usda_food_gold_barcode`; merge then
  completed in seconds.
- Added `idx_foods_source_barcode` for the app's primary lookup path
  (search/scan/recipe building all read through `foods_source.barcode`).

## Final counts
- usda: 553,100
- off: 4,169,963 (4,535,493 total − 336,139 null name − 29,391 barcode collisions)
- foods_source total: 4,723,063

Cross-checked against known collision count from silver-stage analysis
(29,625) — small delta (~234) explained by overlap with the null name
exclusion. No red flags.

**Phase 2 (Silver → Gold) complete.**

Next session: Phase 3 — users table, password hashing, session-state login.
