# 09/16/2026

## Done
- Wrote `get_daily_rollup(conn, user_id, log_date)` in `meal_logging.py`:
  one aggregate query (no GROUP BY, and COALESCE to 0 so an empty day still
  returns one row) plus a meal-names query ordered by `logged_at, id`
  (`now()` is transaction start time, so `id` breaks ties)
- Returns dict: `kcal`, `protein_g`, `carbs_g`, `fat_g`, `meal_names`
  (NULL names → "Untitled", empty list if nothing logged)
- Rewrote `test_meal_logging.py` (with Claude) to cover all of
  `meal_logging.py`, 35 checks, all passing:
  - `search_foods`: 20 row cap, case insensitive match, manual food
    scoping between two users, injection shaped term
  - `log_ad_hoc_meal`: happy path (source + manual food, snapshot
    macros, items), 8 rejection cases each checked for ValueError AND
    no rows written
  - `get_daily_rollup`: empty day, two-meal sum + order, other-user and
    adjacent-date exclusion, NULL name -> "Untitled"
- Test data uses Jan 1999 dates + a temp user and manual foods, all
  removed in a `finally` block
