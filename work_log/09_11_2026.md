# 09/11/2026

## Recipe per-portion macros query

- Wrote `src/sql/gold/recipe_per_portion_macros.sql`: joins `recipes` ->
  `recipe_items` -> `foods` on the composite `(food_id, food_origin)` key,
  sums each ingredient's macro contribution (`kcal * grams / 100.0`, etc.),
  divides by `yield_portions`, grouped by `r.id, r.name, r.yield_portions`.
- Registered a test user via `auth.register_user` (peer auth, no `host=`
  needed — TCP auth requires a password we hadn't set).
- Inserted a test recipe (`user_id 3`, "Test Recipe", 4 portions) and one
  `recipe_items` row (chicken breast, food_id 430, 200g).
- Verified output against manually calculated values: kcal 67, protein 12.945,
  carbs 0, fat 1.56 — all matched.

## Next
- Add a second `recipe_items` row to the test recipe to confirm the
  `SUM()`/`GROUP BY` correctly aggregates multiple ingredients into one row.
- Then: ad hoc logging path (search -> add grams -> save into `meal_log` +
  `meal_log_items`), followed by the daily rollup query.
