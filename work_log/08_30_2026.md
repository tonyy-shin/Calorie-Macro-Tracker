## [08/30/2026] — OFF bronze ingestion + disk resize

- Built `off_product` bronze table (211 TEXT columns + loaded_at) at
  src/sql/bronze/create_off_product.sql, generated from the OFF CSV
  header. Hyphens in source column names (e.g. energy-kcal_100g)
  converted to underscores for valid Postgres identifiers.
- Proved the \copy pattern on a 1000-row sample first: tab-delimited,
  QUOTE E'\x01' to disable CSV quote-parsing (OFF export doesn't
  reliably follow CSV quoting rules). Sample loaded clean — 1000 rows,
  readable text, no encoding errors. Nutrient completeness in the
  sample was low (~5.5%), but that's expected sparsity from
  early/legacy OFF entries at the head of the file, not a load issue.
- Ran out of disk space before the full 9GB load — repartitioned the
  drive via GParted Live USB, shrinking Windows from 378GB to 100GB
  and growing the Linux partition from 97.5GB to 375.5GB. Confirmed
  post-resize with df -h / lsblk — 265GB now available on /.
- Windows partition will show a "needs check" flag on next Windows
  boot after the NTFS shrink — expected, resolves itself.

**Next session starts here:**
1. `psql -h localhost -U tonyyshin -d meal_tracker`
2. `TRUNCATE off_product;`
3. Run the full \copy load from
   en.openfoodfacts.org.products.csv (command saved — same column
   list and options as the sample load, just pointed at the full file
   instead of off_sample.csv)
4. `SELECT count(*) FROM off_product;` to confirm row count
5. Once loaded: Phase 1 (bronze) is fully done — USDA + OFF both
   landed. Decide: Phase 2 silver transforms, or something else.