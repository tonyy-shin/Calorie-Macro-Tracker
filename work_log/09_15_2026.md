# Work Log — 09/15/2026

## Phase 5 — Ad hoc logging path

### Written
- Wrote `log_ad_hoc_meal(conn, user_id, log_date, meal_name, items)` in `src/python/meal_logging.py`
  - Input guard in Python: rejects empty `items`, invalid `food_origin`, and `grams null or grams <= 0`
  - Validation query: `LEFT JOIN` of `unnest()`-ed items against the `foods` view on `(id, origin)` with the ownership filter (`origin = 'source' OR user_id = %s`); raises `ValueError` 
   if any item is missing, not owned by the user, or has a NULL macro
  - Header insert: `INSERT ... SELECT` computes snapshot macros in SQL (`SUM(macro * grams / 100.0)`) and inserts one `meal_log` row with `source_type = 'ad_hoc'`, using `RETURNING id`
  - Items insert: one `meal_log_items` row per item via `unnest()`
  - Validation and both inserts run inside a single `conn.transaction()` block so a failure writes nothing
- Had Claude Code write `src/python/test_meal_logging.py` to test `log_ad_hoc_meal()`

### Test results (all pass)
- Happy path: 150g loaded potato salad + 80g dried mangos logged as one meal; saved 601.0 kcal matched the hand-computed total; 2 `meal_log_items` rows
- Missing macros: food with a NULL macro rejected (`ValueError`)
- Bad reference: nonexistent manual food rejected (`ValueError`)
- Bad grams: negative grams rejected before touching the database
- Failed calls wrote no rows; test meal cleaned up afterward

### Environment notes
- `ModuleNotFoundError: No module named 'psycopg'` came from running without the venv; fixed with `source ~/venv/bin/activate`
- Test connection uses `autocommit=True` so `conn.transaction()` is a real transaction rather than a savepoint

## Next
- Daily rollup function: `COALESCE(SUM(...), 0)` over `meal_log` for a user and date, plus a list of that day's `meal_name`s for the Home tab
